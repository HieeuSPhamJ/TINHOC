#include<bits/stdc++.h>
using namespace std;

int main(){
    freopen("UCLN.INP", "r", stdin);
    freopen("UCLN.OUT", "w", stdout);
    long long a, b;
    cin >> a >> b;
    cout << __gcd(a,b);
    
    return 0;
}