#include<bits/stdc++.h>
#define ii pair <int,int>
#define f first
#define s second
#define ll long long
using namespace std;

const int inf = 1e18;
const int maxN = 2 * 1e5 + 10;
int valCom[maxN];
int father[maxN];
vector <ii> total

signed main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n, m, q;
    cin >> n >> m >> q;
    for (int i = 1; i <= n; i++){
        cin >> valCom[i];
        father[i] = i;
    }

    for (int i = 1; i <= n; i++){

    }
    
    return 0;
}