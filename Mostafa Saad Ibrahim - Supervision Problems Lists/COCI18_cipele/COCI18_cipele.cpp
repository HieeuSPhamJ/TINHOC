#include<bits/stdc++.h>
#define ii pair <int,int>
#define fi first
#define se second
#define int long long
using namespace std;


vector <int> listA;
vector <int> listB;

signed main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n, m;
    cin >> n >> m;
    for (int i = 1; i <= n; i++){
        int inp;
        cin >> inp;
        listA.push_back(inp);
    }
    for (int i = 1; i <= m; i++){
        int inp;
        cin >> inp;
        listB.push_back(inp);
    }

    sort()

    return 0;
}