#include<bits/stdc++.h>
using namespace std;

const int maxN=2*1e5;
const int maxM=1e9;

int a[maxN];
int b[maxN];

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin>>n;
    // cout<<n;
    for (int i=1;i<=n;i++){
        cin>>a[i]>>b[i];
    }
    bool checka[maxM]={false};
    bool checkb[maxM]={false};
    for (int i=1;i<=n;i++){
        
    }
    cout<<ans;
    return 0;
}